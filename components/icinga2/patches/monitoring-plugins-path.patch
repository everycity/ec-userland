diff -ruN icinga2-2.4.1-bin.orig/ec/etc/icinga2/constants.conf icinga2-2.4.1-bin/ec/etc/icinga2/constants.conf
--- icinga2-2.4.1-bin.orig/ec/etc/icinga2/constants.conf	2015-12-16 18:19:59.000000000 +0000
+++ icinga2-2.4.1-bin/ec/etc/icinga2/constants.conf	2015-12-18 13:58:11.605315477 +0000
@@ -4,17 +4,17 @@
  */
 
 /* The directory which contains the plugins from the Monitoring Plugins project. */
-const PluginDir = "/usr/lib/nagios/plugins"
+const PluginDir = "/ec/lib/nagios/plugins"
 
 /* The directory which contains the Manubulon plugins.
  * Check the documentation, chapter "SNMP Manubulon Plugin Check Commands", for details.
  */
-const ManubulonPluginDir = "/usr/lib/nagios/plugins"
+const ManubulonPluginDir = "/ec/lib/nagios/plugins"
 
 /* The directory which you use to store additional plugins which ITL provides user contributed command definitions for.
  * Check the documentation, chapter "Plugins Contribution", for details.
  */
-const PluginContribDir = "/usr/lib/nagios/plugins"
+const PluginContribDir = "/ec/lib/nagios/plugins"
 
 /* Our local instance name. By default this is the server's hostname as returned by `hostname --fqdn`.
  * This should be the common name from the API certificate.
