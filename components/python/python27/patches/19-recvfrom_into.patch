--- Python-2.7.13/Modules/socketmodule.c.~1~	2016-12-17 20:05:07.000000000 +0000
+++ Python-2.7.13/Modules/socketmodule.c	2017-01-09 15:58:37.536247556 +0000
@@ -2768,6 +2768,13 @@
         goto error;
     }
 
+    /* Check if the buffer is large enough */
+    if (buflen < recvlen) {
+        PyErr_SetString(PyExc_ValueError,
+                        "buffer too small for requested bytes");
+        goto error;
+    }
+
     readlen = sock_recvfrom_guts(s, buf.buf, recvlen, flags, &addr);
     if (readlen < 0) {
         /* Return an error */
